module.exports = {
  plugins: [
    require('tailwindcss'),   // Use 'tailwindcss' directly (not @tailwindcss/postcss)
    require('autoprefixer'),   // autoprefixer for cross-browser support
  ],
};
